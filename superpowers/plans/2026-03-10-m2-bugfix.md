# M2 Bug Fix Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 M2 (src/pages/m2/) 六页中已发现的 UX/布局 bug，并举一反三覆盖同类问题，同时优化 AI 图片占位符的空间规划。

**Architecture:** 逐文件修复，每个 Task 对应一个 JS 源文件。所有修改均在 render() 的 `<style>` 块或 init() 的 JS 逻辑中进行，无需新增文件。修复后 git commit。

**Tech Stack:** Vanilla JS, GSAP, D3 (CDN global), Mermaid (CDN ESM), CSS custom properties, Vite

---

## 文件修改总览

| 文件 | 修改类型 | Bug |
|------|---------|-----|
| `src/pages/m2/p01-ai-overview.js` | CSS + JS data | 工具矩阵冗余文字 + 缺少 Nano Banana |
| `src/pages/m2/p02-prompt-engineering.js` | JS init | CDTF 左侧面板顶部偏移不足 |
| `src/pages/m2/p04-ethics.js` | CSS + JS | Mermaid 图在移动端变得过小 |
| `src/pages/m2/p05-ai-diagrams.js` | CSS + JS | 期刊政策表移动端溢出 + 折叠框宽度跳变 |
| `src/pages/m2/p06-case-studies.js` | CSS + JS | Mermaid 流程图过高难以浏览 + GA 占位符比例 |

---

## Chunk 1: p01 + p02 修复

### Task 1: p01 工具矩阵文字清理 + Nano Banana 补充

**Files:**
- Modify: `src/pages/m2/p01-ai-overview.js`

**背景：**
Section S3（工具生态速览）副标题有"，不是逐工具介绍"这句无意义的元信息，需删除。工具矩阵缺少最重要的 Nano Banana（Gemini on OpenRouter），是本项目专用的 AI 图像生成工具。

- [ ] **Step 1: 修改 S3 section 副标题**

在 `render()` 的 HTML 中定位 S3 section：
```html
<!-- 原文（约 line 147） -->
<p style="color:var(--text-on-dark-2);margin-top:var(--space-sm);font-size:var(--text-body);line-height:1.6;">按任务类型理解工具能力，不是逐工具介绍</p>

<!-- 改为 -->
<p style="color:var(--text-on-dark-2);margin-top:var(--space-sm);font-size:var(--text-body);line-height:1.6;">按任务类型理解各工具的能力边界</p>
```

- [ ] **Step 2: 在 TOOLS 数组末尾添加 Nano Banana**

在 `init()` 中定位 `const TOOLS = [...]`（约 line 298），修改为：
```js
const TOOLS = ['GPT Image 1.5','Midjourney v7','FLUX 1.1 Pro','Recraft V3 SVG','StarVector','Mermaid AI','Nano Banana'];
```

- [ ] **Step 3: 在 SCORES 数组末尾添加 Nano Banana 评分行**

```js
const SCORES = [
  [2,1,0,2],  // GPT Image 1.5
  [2,0,0,2],  // Midjourney v7
  [2,0,0,2],  // FLUX 1.1 Pro
  [2,1,1,2],  // Recraft V3 SVG
  [1,2,0,1],  // StarVector
  [0,2,0,0],  // Mermaid AI
  [2,1,0,2],  // Nano Banana ← 新增
];
```

- [ ] **Step 4: 在 DESCS 数组末尾添加 Nano Banana 描述行**

```js
const DESCS = [
  // ... 原有6行保持不变 ...
  // Nano Banana（第7行，新增）：
  ['基于 Gemini 模型，特别擅长环境科学场景概念图生成，本项目主要使用工具','可生成简单流程图，结构稳定可控','不适合生成含精确数值的数据图表','可生成高质量科研图标和场景素材'],
];
```

- [ ] **Step 5: 在浏览器中验证**

```
npm run dev
```
打开 #m2-p1 → 滚动到「工具生态速览（2026）」：
- 副标题应为「按任务类型理解各工具的能力边界」（不含"不是逐工具介绍"）
- 矩阵应有 7 行，最后一行为 Nano Banana，显示正确的 ✅⚠️❌ 评分
- 鼠标悬停 Nano Banana 单元格应显示 tooltip

- [ ] **Step 6: Commit**

```bash
git add src/pages/m2/p01-ai-overview.js
git commit -m "fix(m2-p1): 清理工具矩阵副标题，补充 Nano Banana 工具行"
```

---

### Task 2: p02 CDTF 粘性滚动左侧面板顶部偏移修复

**Files:**
- Modify: `src/pages/m2/p02-prompt-engineering.js:263-311`

**背景：**
CDTF Prompt 解剖模块使用 JS 模拟 `position:sticky`（因 body overflow-x:hidden 导致 CSS sticky 失效）。当前实现当 body 容器顶部到达视口顶部时（`bodyRect.top = 0`），translateY = 0，左侧面板贴在视口最顶端，滚动时"挤在最上面"体验差。需要增加 80px 的顶部偏移，使面板出现在距视口顶部约 80px 的位置。

**修复逻辑：**
- `maxTrans` 需减去 `TOP_OFFSET` 以防面板超出 body 容器底部
- 只在真正进入粘性区域（`bodyRect.top < 0`）时才应用偏移
- 在 translateY 计算中叠加 `TOP_OFFSET`

- [ ] **Step 1: 修改 init() 中 CDTF sticky 逻辑（约 line 265-311）**

在 `if (window.innerWidth > 900)` 块内修改如下（替换整个 if 块的内容）：

```js
if (window.innerWidth > 900) {
  const bodyEl = document.getElementById('p02-cdtf-body');
  const leftEl = document.getElementById('p02-cdtf-left');
  const panels = document.querySelectorAll('.p02-cdtf-panel');
  const segs   = document.querySelectorAll('.p02-seg');
  if (bodyEl && leftEl && panels.length) {
    const PANEL_COUNT = panels.length; // 4
    const TOP_OFFSET = 80; // px from viewport top when panel is "stuck"
    let ticking = false;
    let currentPanel = 0;
    // Cache once after DOM render to avoid repeated layout reflows on scroll
    let cachedBodyH = bodyEl.offsetHeight;
    let cachedLeftH = leftEl.offsetHeight;
    // Subtract TOP_OFFSET so panel bottom never exceeds body bottom
    let maxTrans = Math.max(0, cachedBodyH - cachedLeftH - TOP_OFFSET);

    // Recompute on resize so sticky scroll stays correct after viewport changes
    let resizeTimer = null;
    const onResize = () => {
      clearTimeout(resizeTimer);
      resizeTimer = setTimeout(() => {
        cachedBodyH = bodyEl.offsetHeight;
        cachedLeftH = leftEl.offsetHeight;
        maxTrans = Math.max(0, cachedBodyH - cachedLeftH - TOP_OFFSET);
      }, 150);
    };
    window.addEventListener('resize', onResize, { passive: true });
    _scrollHandlers.push({ fn: onResize, el: window, event: 'resize' });

    function updateCDTF() {
      if (ticking) return;
      ticking = true;
      requestAnimationFrame(() => {
        const bodyRect = bodyEl.getBoundingClientRect();
        // scrolledPast > 0 when body top has crossed viewport top (sticky active)
        const scrolledPast = Math.max(0, -bodyRect.top);
        // TOP_OFFSET: always added so panel appears 80px below viewport top during sticky.
        // Before sticky, element is below the fold so the offset has no visible effect.
        leftEl.style.transform = `translateY(${TOP_OFFSET + Math.min(scrolledPast, maxTrans)}px)`;
        const panelIdx = Math.min(PANEL_COUNT - 1, Math.max(0, Math.floor(scrolledPast / window.innerHeight)));
        if (panelIdx !== currentPanel) {
          currentPanel = panelIdx;
          panels.forEach((p, i) => p.classList.toggle('active', i === panelIdx));
          // segs[0] = intro line, segs[1..4] = C/D/T/F — offset by 1
          segs.forEach((s, i) => s.classList.toggle('active', i === panelIdx + 1));
        }
        ticking = false;
      });
    }
    window.addEventListener('scroll', updateCDTF, { passive: true });
    _scrollHandlers.push({ fn: updateCDTF, el: window });
  }
}
```

- [ ] **Step 2: 验证**

打开 #m2-p2 → 滚动进入 CDTF 解剖 section：
- 左侧 Prompt 框不应贴在视口最顶端，应距顶约 80px
- 继续滚动时面板跟随右侧内容切换 C/D/T/F 高亮，左侧框流畅向下移动
- 滚动到 section 底部时，左侧框底部与 section 底部对齐，不超出
- 移动端（≤900px）不受影响（左侧框为静态布局）

- [ ] **Step 3: Commit**

```bash
git add src/pages/m2/p02-prompt-engineering.js
git commit -m "fix(m2-p2): CDTF 粘性左面板添加 80px 顶部偏移，改善滚动体验"
```

---

## Chunk 2: p04 + p05 修复

### Task 3: p04 Mermaid 图移动端可滚动修复

**Files:**
- Modify: `src/pages/m2/p04-ethics.js`

**背景：**
p04 S2 有三个 Mermaid 场景（污水处理工艺 LR / PM2.5 源解析 TD / 碳通量监测 LR）。在移动端：
- LR（横向）流程图节点多，SVG 天然宽度超过视口，当前 `max-width:100%` 会压缩 SVG 至视口宽度，导致文字变得极小甚至不可辨认
- TD（纵向）流程图高度较高，当前无高度限制，可能撑高整个页面

修复方案：
1. 在 `.p04-mermaid-wrap` CSS 中增加 `max-height: 480px`（含 mobile 降级为 320px），使容器在 `overflow:auto` 下可纵向滚动
2. 在 `renderMermaid` 函数中，把 SVG 的 `max-width:100%;height:auto;` 改为 `display:block;height:auto;`（移除 max-width 限制），让 SVG 保持其天然宽度，配合容器的 `overflow:auto` 实现横向/纵向滚动

- [ ] **Step 1: 在 CSS 的 .p04-mermaid-wrap 中添加 max-height**

定位 render() 中 `<style>` 块（约 line 269）：
```css
/* 原 */
.p04-mermaid-wrap { background:var(--bg-dark-elevated); border-radius:var(--radius-lg); padding:var(--space-lg); border:1px solid var(--border-dark); min-height:200px; margin-bottom:var(--space-lg); overflow:auto; }

/* 改为 */
.p04-mermaid-wrap { background:var(--bg-dark-elevated); border-radius:var(--radius-lg); padding:var(--space-lg); border:1px solid var(--border-dark); min-height:200px; max-height:480px; margin-bottom:var(--space-lg); overflow:auto; -webkit-overflow-scrolling:touch; }
```

在 `@media (max-width:768px)` 块中追加（约 line 297）：
```css
@media (max-width:768px) {
  #p04-s1, #p04-s2, #p04-s3, #p04-s4 { scroll-margin-top:56px; }
  .p04-type-grid { grid-template-columns:repeat(2,1fr); }
  .p04-collab-grid { grid-template-columns:1fr; }
  .p04-mermaid-wrap { max-height:320px; }  /* ← 新增 */
}
```

- [ ] **Step 2: 修改 renderMermaid 函数中的 SVG 样式**

定位 `renderMermaid` 函数（约 line 120-145）中的行：
```js
// 原
container.querySelector('svg')?.setAttribute('style', 'max-width:100%;height:auto;');

// 改为：移除 max-width 限制，让 SVG 保持天然宽度，容器 overflow:auto 提供滚动
const svgEl = container.querySelector('svg');
if (svgEl) {
  svgEl.style.display = 'block';
  svgEl.style.height = 'auto';
  // 不设 max-width：LR 流程图保持天然宽度，可横向滚动阅读；TD 流程图纵向可滚动
}
```

- [ ] **Step 3: 在 Mermaid 容器下方添加滚动提示文字（移动端）**

在 render() 的 HTML 中，定位 `.p04-mermaid-wrap` 的 HTML（约 line 350）。该 div 后面紧跟着 `.p04-code-wrap`（含复制按钮和 `<pre>` 代码块）。**提示文字必须插在 `.p04-mermaid-wrap` 关闭 div 和 `.p04-code-wrap` 开始 div 之间**，不要放在代码块之后：

```html
<!-- 示意位置（在 mermaid-wrap 关闭之后、code-wrap 开始之前） -->
<div class="p04-mermaid-wrap" id="p04-mermaid-output">...</div>
<p class="p04-mermaid-hint">← 可滑动查看完整流程图 →</p>  <!-- ← 插入此行 -->
<div class="p04-code-wrap" id="p04-code-wrap">...</div>
```

添加 CSS（在 `<style>` 块中，桌面端隐藏，仅移动端显示）：
```css
.p04-mermaid-hint { display:none; }
@media (max-width:768px) { .p04-mermaid-hint { display:block; text-align:center; font-size:var(--text-caption); color:var(--text-on-dark-3); margin-bottom:var(--space-sm); } }
```

- [ ] **Step 4: 同步升高桌面端 max-height 至 600px**

p04 场景 2「PM2.5 源解析」是 TD（纵向）14 节点流程图，天然高度很可能超过 480px，在桌面端也会被裁切。将桌面端 max-height 调高至 600px（仅改 `.p04-mermaid-wrap` 初始值）：

```css
/* 改为 600px（桌面端）+ 320px（移动端） */
.p04-mermaid-wrap { ... max-height:600px; ... }

/* @media (max-width:768px) 中保持 */
.p04-mermaid-wrap { max-height:320px; }
```

- [ ] **Step 5: 验证**

打开 #m2-p4 → 切换三个 Mermaid 场景 Tab：
- **场景 1/3（LR）**：桌面端全高显示不被裁切；移动端可横向滑动，文字可辨认
- **场景 2（TD）**：桌面端在 600px 容器内可滑动；移动端 320px 容器内可滑动
- 移动端：`.p04-mermaid-hint` 提示文字显示在流程图下方、代码块上方
- 桌面端：提示文字隐藏

- [ ] **Step 6: Commit**

```bash
git add src/pages/m2/p04-ethics.js
git commit -m "fix(m2-p4): Mermaid 容器 max-height 600px/320px，SVG 移除 max-width 限制，移动端可滚动浏览"
```

---

### Task 4: p05 期刊政策表移动端溢出 + 折叠框宽度跳变修复

**Files:**
- Modify: `src/pages/m2/p05-ai-diagrams.js`

**背景：**
存在两个独立 bug：

**Bug A：期刊政策表移动端溢出**
S1 期刊政策表在移动端显示时会超出屏幕宽度。表格已有 `overflow-x:auto` wrapper，但 section 的 `padding: var(--space-3xl) var(--space-xl)` 在移动端占用了 32px×2=64px 的水平空间，加之外层 `max-width:900px` div，可用空间太窄（375-64=311px），表格 `min-width:600px` 虽理论上可滚动，但实际因 section 本身可能出现横向溢出而导致整页超宽。

解决方案：移动端使用负 margin 让 table-wrap 全宽（edge-to-edge），更多空间供 600px 表格在其中滚动。

**Bug B：折叠框展开时宽度跳变**
S2「使用 AI 的五个层级」折叠框在展开/收起时，行宽会变化。原因：`.p05-spectrum-row` 和 `.p05-spectrum-detail` 没有明确的 `box-sizing:border-box` 和 `overflow:hidden`，detail 文本显示/隐藏时可能导致 layout 重算引发宽度跳变。

- [ ] **Step 1: 在 CSS 中添加 table-wrap 移动端全宽修复**

在 render() `<style>` 块中，定位 `@media (max-width:768px)` 块（约 line 180），添加：

```css
@media (max-width:768px) {
  #p05-s1, #p05-s2, #p05-s3, #p05-s4 { scroll-margin-top:56px; }
  .p05-quiz-grid { grid-template-columns:1fr; }
  /* ↓ 新增：期刊政策表全宽（负 margin 抵消 section 水平 padding）
     注：section 在移动端的水平 padding 为 var(--space-xl) = 32px（见 variables.css）
     使用固定 32px 而非 CSS 变量，避免变量在 @media 块中解析不一致 */
  .p05-table-wrap {
    margin-left: -32px;
    margin-right: -32px;
    border-radius: 0;
    border-left: none;
    border-right: none;
  }
}
```

- [ ] **Step 2: 在 CSS 中修复折叠框宽度跳变**

在 `<style>` 块中，修改 `.p05-spectrum-row` 和相关 CSS：

```css
/* 原 */
.p05-spectrum-row { border-radius:var(--radius-md); margin-bottom:var(--space-sm); cursor:pointer; border-left:4px solid; transition:opacity 0.2s; }

/* 改为（添加 overflow:hidden 和 box-sizing） */
.p05-spectrum-row { border-radius:var(--radius-md); margin-bottom:var(--space-sm); cursor:pointer; border-left:4px solid; transition:opacity 0.2s; overflow:hidden; box-sizing:border-box; width:100%; }
```

同时修改 `.p05-spectrum-detail` 添加换行保护：
```css
/* 原 */
.p05-spectrum-detail { padding:0 var(--space-md) var(--space-md); font-size:0.88rem; color:var(--text-on-dark-2); line-height:1.7; border-top:1px solid rgba(255,255,255,0.08); }

/* 改为 */
.p05-spectrum-detail { padding:0 var(--space-md) var(--space-md); font-size:0.88rem; color:var(--text-on-dark-2); line-height:1.7; border-top:1px solid rgba(255,255,255,0.08); overflow-wrap:break-word; word-break:break-word; box-sizing:border-box; }
```

- [ ] **Step 3: 修复移动端弹出框定位**

当前点击表格单元格时，弹出框使用 `position:fixed`，定位逻辑基于 `rect.left/bottom + window.scrollX/Y`。在移动端表格横向滚动后，`rect.left` 相对于视口是正确的，但弹出框宽度 320px 可能超出屏幕。在已有的弹出框定位代码（约 line 421-428）中确认已有 `if (left + pw > window.innerWidth - 16) left = window.innerWidth - pw - 16`。

此逻辑基本正确，但需补充：当 `pw > window.innerWidth - 32`（即弹窗比屏幕还宽，极小屏）时，缩小弹窗宽度。

在 popup 的 CSS 中修改：
```css
/* 原 */
.p05-policy-popup { position:fixed; background:var(--bg-light-elevated,#fff); border:1px solid var(--border-light); border-radius:var(--radius-md); padding:var(--space-md); max-width:320px; ... }

/* 改为（添加 min-width 和移动端全宽保护） */
.p05-policy-popup { position:fixed; background:var(--bg-light-elevated,#fff); border:1px solid var(--border-light); border-radius:var(--radius-md); padding:var(--space-md); max-width:min(320px, calc(100vw - 32px)); ... }
```

- [ ] **Step 4: 验证**

打开 #m2-p5：

**表格测试（移动端 375px）：**
- 期刊政策表应 edge-to-edge 显示，不留白边
- 表格可横向滑动（min-width:600px 在全宽容器中滚动）
- 第一列（期刊名）sticky 固定不动

**折叠框测试：**
- 点击「完全安全」展开，行宽不应跳变
- 点击再次收起，宽度同样稳定
- 快速连续展开/收起应无视觉抖动

- [ ] **Step 5: Commit**

```bash
git add src/pages/m2/p05-ai-diagrams.js
git commit -m "fix(m2-p5): 期刊表移动端全宽，折叠框展开宽度稳定"
```

---

## Chunk 3: p06 修复

### Task 5: p06 Mermaid 流程图高度限制 + GA 占位符比例优化

**Files:**
- Modify: `src/pages/m2/p06-case-studies.js`

**背景：**
**Bug A：Mermaid 流程图过高**
S3 的微塑料迁移路径流程图（CASE2_MERMAID）是 `flowchart TD`（纵向）共 12 个节点，渲染后 SVG 高度超过 1000px，占满多个屏幕，用户需要滚动很久才能离开这个图。需要给 `.p06-mermaid-wrap` 添加 max-height 上限，使其在 `overflow:auto` 下可纵向滚动浏览，而不是撑开整个页面。同时和 p04 一致，移除 SVG 的 `max-width:100%` 限制。

**Bug B：GA 占位符比例不符合实际图像尺寸**
S2 案例一（水污染 GA）的 Before/After 占位符使用 `aspect-ratio:4/3`（4:3≈1.33:1），但实际 Graphical Abstract 通常为宽幅格式（1.7:1 ~ 2:1 之间）。当真实 GA 图片替换占位符时布局会出现比例不匹配。改为 `aspect-ratio:16/9`（≈1.78:1）更接近实际。

- [ ] **Step 1: 在 CSS 中给 .p06-mermaid-wrap 添加 max-height**

定位 render() `<style>` 块中的 `.p06-mermaid-wrap`（约 line 255）：
```css
/* 原 */
.p06-mermaid-wrap { background:var(--bg-dark-elevated); border-radius:var(--radius-lg); padding:var(--space-lg); border:1px solid var(--border-dark); min-height:200px; overflow:auto; max-width:900px; margin:0 auto var(--space-2xl); }

/* 改为（添加 max-height 和 -webkit-overflow-scrolling） */
.p06-mermaid-wrap { background:var(--bg-dark-elevated); border-radius:var(--radius-lg); padding:var(--space-lg); border:1px solid var(--border-dark); min-height:200px; max-height:500px; overflow:auto; -webkit-overflow-scrolling:touch; max-width:900px; margin:0 auto var(--space-2xl); }
```

在 `@media (max-width:768px)` 块（约 line 286）中添加：
```css
@media (max-width:768px) {
  #p06-s1, #p06-s2, #p06-s3, #p06-s4, #p06-s5 { scroll-margin-top:56px; }
  .p06-steps-grid.cols-4 { grid-template-columns:1fr; }
  .p06-steps-grid.cols-5 { grid-template-columns:1fr; }
  .p06-mermaid-wrap { max-height:360px; }  /* ← 新增 */
}
```

- [ ] **Step 2: 在 Mermaid 容器下方添加滚动提示（移动端可见）**

在 CSS `<style>` 块中添加：
```css
.p06-mermaid-hint { display:none; }
@media (max-width:768px) { .p06-mermaid-hint { display:block; text-align:center; font-size:var(--text-caption); color:var(--text-on-dark-3); margin-top:calc(-1 * var(--space-md)); margin-bottom:var(--space-md); } }
```

在 render() HTML 中，定位 Mermaid 容器（约 line 375-377）：
```html
<!-- 原 -->
<div class="p06-mermaid-wrap" id="p06-mermaid-output">
  <div style="color:var(--text-on-dark-3);...">正在加载 Mermaid 流程图…</div>
</div>

<!-- 改为（在 div 之后添加提示） -->
<div class="p06-mermaid-wrap" id="p06-mermaid-output">
  <div style="color:var(--text-on-dark-3);font-size:0.85rem;text-align:center;padding:var(--space-lg);">正在加载 Mermaid 流程图…</div>
</div>
<p class="p06-mermaid-hint">← 可滑动浏览完整流程图 →</p>
```

- [ ] **Step 3: 修改 renderMermaid 函数中的 SVG 样式**

定位 `renderMermaid` 函数（约 line 32-49）中的行：
```js
// 原
container.querySelector('svg')?.setAttribute('style', 'max-width:100%;height:auto;');

// 改为
const svgEl = container.querySelector('svg');
if (svgEl) {
  svgEl.style.display = 'block';
  svgEl.style.height = 'auto';
  // 不设 max-width：SVG 保持天然宽度，容器 overflow:auto 负责滚动
}
```

- [ ] **Step 4: 修改 S2 BA 行的 GA 占位符比例**

在 render() HTML 中，定位 S2 Before/After 行（约 line 340-353）：
```html
<!-- 原：两处 aspect-ratio:4/3 -->
<div style="aspect-ratio:4/3;">
  ${buildPlaceholder('🖊️', '原始草图 / 参考图占位符', ...)}
</div>
...
<div style="aspect-ratio:4/3;">
  ${buildPlaceholder('🌊', '最终成果 GA 占位符', ...)}
</div>

<!-- 改为：16/9（更接近 GA 实际宽幅格式） -->
<div style="aspect-ratio:16/9;">
  ${buildPlaceholder('🖊️', '原始草图 / 参考图占位符', ...)}
</div>
...
<div style="aspect-ratio:16/9;">
  ${buildPlaceholder('🌊', '最终成果 GA 占位符', ...)}
</div>
```

同时，S4 案例三碳循环概念图占位符保持 `aspect-ratio:4/3`（综述概念图通常接近方形到 4:3，无需改动）。S3 方法流程图最终成果占位符保持 `aspect-ratio:4/3`（流程图通常高于宽，4:3 合适）。

- [ ] **Step 5: 验证**

打开 #m2-p6：

**Mermaid 测试：**
- S3「微塑料迁移路径流程图」应在 500px 高的容器内显示，超出部分可纵向滚动
- 移动端提示"← 可滑动浏览完整流程图 →"可见
- 桌面端提示隐藏

**GA 占位符测试：**
- S2 Before/After 两个占位符应为宽幅（16:9），视觉上比之前更像实际 GA 图像的尺寸

- [ ] **Step 6: Commit**

```bash
git add src/pages/m2/p06-case-studies.js
git commit -m "fix(m2-p6): Mermaid 流程图添加 max-height 可滚动，GA 占位符改为 16/9 宽幅比例"
```

---

## Chunk 4: 举一反三 — 跨页一致性检查 + 收尾

### Task 6: 举一反三 — 检查并修复 p03 同类问题

**Files:**
- Modify: `src/pages/m2/p03-vectorization.js`（如有必要）

**背景：**
根据前面修复的经验，p03 可能有类似的移动端问题：
1. D3 SVG 模拟器 — 是否有 `max-width:100%` 约束？
2. 三路径 Tab 内容是否有移动端宽度溢出？
3. 5张问题卡片滚动区域是否有 `scroll-margin-top:56px`？

- [ ] **Step 1: 审查 p03 并列出问题**

读取 `src/pages/m2/p03-vectorization.js`，检查：
1. D3 SVG 容器是否有 `overflow:auto` + 合适尺寸（容器 class 通常为 `p03-sim-wrap` 或类似）
2. `@media (max-width:768px)` 块是否包含所有 section 的 `scroll-margin-top:56px`
3. 三路径 Tab 内容是否有宽度溢出风险

- [ ] **Step 2: 执行修复（仅修复已确认的问题）**

**如发现 scroll-margin-top 缺失**，在 `@media (max-width:768px)` 块中添加：
```css
#p03-s1, #p03-s2, #p03-s3, #p03-s4 { scroll-margin-top:56px; }
```
（section ID 以实际文件中定义的为准，替换上方对应的 ID。）

**如发现 D3 SVG 容器无 overflow:auto**，在容器 CSS 中添加：
```css
.p03-sim-wrap { overflow:auto; -webkit-overflow-scrolling:touch; }
/* 其中 .p03-sim-wrap 替换为实际的 D3 容器类名 */
```

**如发现 D3 SVG 本身有 max-width:100% 导致缩小**，将其改为 `min-width:300px; max-width:100%;`（D3 图不同于 Mermaid，D3 通常已通过 viewBox 自适应，只需确保不被过度压缩）。

不引入未经确认的改动。

- [ ] **Step 3: 验证**

移动端 (Chrome DevTools iPhone SE 375px) 检查 #m2-p3 各 section 是否正常。

- [ ] **Step 4: Commit（如有改动）**

```bash
git add src/pages/m2/p03-vectorization.js
git commit -m "fix(m2-p3): 修复移动端同类问题（scroll-margin / SVG overflow）"
```

---

### Task 7: 最终验证 + todo.md 更新

**Files:**
- Modify: `todo.md`

- [ ] **Step 1: 全量移动端检查**

使用 `npm run dev`，在 Chrome DevTools 中依次用 iPhone SE (375px) 测试所有 6 个 M2 页面：

| 页面 | 检查项 |
|------|--------|
| m2-p1 | 工具矩阵有 Nano Banana；副标题无冗余文字 |
| m2-p2 | CDTF 左面板距视口顶 ~80px，不贴顶 |
| m2-p3 | D3 模拟器可用；无横向溢出 |
| m2-p4 | Mermaid 三场景可滚动浏览；LR 可横滑，TD 可纵滑 |
| m2-p5 | 期刊政策表全宽横滑；折叠框展开宽度稳定 |
| m2-p6 | 微塑料 Mermaid 图可纵滑；GA 占位符为宽幅 |

- [ ] **Step 2: 在 todo.md 中记录 bug fix 完成**

在 `## Phase 5` 下方、`## Phase 5.5` 之前，添加：
```markdown
## Phase 5.1: M2 Bug Fix（移动端适配 + Mermaid + 矩阵 + 折叠框）
- [x] p01：工具矩阵副标题清理 + 补充 Nano Banana
- [x] p02：CDTF 粘性左面板顶部偏移修复（80px）
- [x] p04：Mermaid 图 max-height + 移除 max-width 限制，移动端可滚动
- [x] p05：期刊政策表移动端全宽 + 折叠框宽度稳定
- [x] p06：Mermaid 流程图 max-height + GA 占位符改为 16/9
- [x] p03：举一反三检查同类问题
```

在进度总览表中添加：
```markdown
| Phase 5.1 M2 Bug Fix | ✅ 完成 | 移动端适配 + Mermaid 优化 + 工具矩阵补充 |
```

- [ ] **Step 3: Final commit**

```bash
git add todo.md
git commit -m "docs: 标记 M2 Phase 5.1 bug fix 完成"
```
